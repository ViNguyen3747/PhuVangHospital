let pick,dotenv,jwt;_f30‍.x([["createActivationToken",()=>createActivationToken],["serializeUser",()=>serializeUser]]);_f30‍.w("lodash",[["pick",["pick"],function(v){pick=v}]]);_f30‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_f30‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);



dotenv.config();

const createActivationToken = async (jwtPayload) => {
  let token = await jwt.sign(jwtPayload, process.env.ACTIVATION_TOKEN_SECRET, {
    expiresIn: 3600 * 24,
  });
  return token;
};

const serializeUser = (user) =>
  pick(user, ["id", "email", "lastName", "firstName", "password", "admin"]);

