let mongoose;_58e‍.x([["default",()=>_58e‍.o]]);_58e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const taskSchema = new mongoose.Schema(
  {
    employee: { type: String, required: true },
    task: [{ type: String }],
    createdAt: {
      type: Date,
      default: new Date(),
      required: true,
    },
    updatedAt: {
      type: Date,
      default: new Date(),
      required: true,
    },
  },
  {
    timestamps: true,
  }
);

const Task = mongoose.model("Task", taskSchema);
_58e‍.d(Task);
